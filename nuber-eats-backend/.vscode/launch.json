{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start:dev",
            "request": "launch",
            "command": "yarn start:dev:hot",
            "sourceMaps": true,
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env.dev"
        }
    ]
}